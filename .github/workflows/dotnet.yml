name: Web Deploy

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '6.x.x'
      - name: Restore dependencies
        run: dotnet restore
      - name: Build
        run: dotnet build $(SolutionDir)/JmkCoder101/JmkCoder101.csproj
      - name: Publish
        env:
          DEPLOY_PASSWD: ${{ secrets.DEPLOY_PASSWD }}
        run: echo $DEPLOY_PASSWD dotnet publish $(SolutionDir)/JmkCoder101/JmkCoder101.csproj /p:PublishProfile=WebDeploy /p:Password=$DEPLOY_PASSWD
